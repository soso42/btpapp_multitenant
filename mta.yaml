_schema-version: "3.1"
ID: btp-app
version: 1.0.0

modules:
  - name: btp-app
    type: java
    path: .
    parameters:
      memory: 1G
      disk-quota: 1G
      routes:
        - route: ss-app.cfapps.us10-001.hana.ondemand.com
    properties:
      SPRING_PROFILES_ACTIVE: cloud
    build-parameters:
      builder: maven
      build-result: target/btp-app-1.jar

